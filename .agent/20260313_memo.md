# ExecPlan 追記内容: コンテナ前提の runtime state 分離

## Summary

既存の ExecPlan `[.agent/2026-03-06-local-knowledge-layer-execplan.md](C:\Users\Hodaka\Downloads\div\local-knowledge-layer\.agent\2026-03-06-local-knowledge-layer-execplan.md)` に、次フェーズの実装予定として「Skill 本体と runtime state の分離」を追記する。目的は、Claude Code / Codex の sandbox / container 前提でも破綻せず、Git 管理対象に知識キャッシュ生成物を混ぜない設計へ移行することにある。追記後の ExecPlan では、現在の `knowledge/` と `index/` の Skill 内包設計を v1 と位置付け、次の改善マイルストーンとして workspace hidden state 方式への移行を明示する。

## Key Changes

- `Purpose / Big Picture` に次の一文を加える。
  現行実装は Skill 配下に knowledge state を持つが、次フェーズでは container/sandbox 前提に合わせて runtime state を workspace 配下の `./.local-knowledge-layer/` へ分離し、Git 管理対象から外す。

- `Progress` に未着手タスクとして次を追加する。
  `runtime state を Skill package から分離する設計変更を次フェーズで実装する`
  `CLI に --state-root と LOCAL_KNOWLEDGE_STATE_DIR を追加する`
  `README / references / .gitignore を workspace hidden state 前提へ更新する`

- `Decision Log` に次の決定を追加する。
  Decision: 次フェーズでは `global` を物理グローバル保存としては扱わず、workspace 内共有の論理層として再定義する。
  Rationale: Claude Code / Codex は sandbox / container / workspace 実行が前提になりやすく、永続グローバル保存を設計前提にすると portability と Git hygiene を損ねるため。
  Date/Author: 2026-03-13 / Codex

  Decision: runtime 生成物の既定保存先は repo ルート直下の `./.local-knowledge-layer/` とする。
  Rationale: current working directory 配下なら sandbox と相性が良く、volume 永続化の有無にかかわらず扱いやすく、`.gitignore` で簡単に除外できるため。
  Date/Author: 2026-03-13 / Codex

  Decision: 公開リポジトリには Skill 本体と最小テンプレートだけを残し、raw/index/refresh-state などの runtime state は Git 管理しない。
  Rationale: 実データや更新差分がリポジトリを汚さないようにするため。
  Date/Author: 2026-03-13 / Codex

- `Plan of Work` に次フェーズ用の短い段落を追加する。
  `scripts/cache_lib.py` を中心に package root と state root を分離し、`ingest_source.py`、`harvest_api_docs.py`、`build_index.py`、`search_cache.py`、`refresh_cache.py` が state root を参照するよう変更する。`references/` と `SKILL.md` は package root に残し、`knowledge/`、`index/`、`manifests/` は state root 側に移す。互換性のため `--root` は Skill package ルートとして残し、新設の `--state-root` を runtime state 用に使う。

- `Validation and Acceptance` に次フェーズの受け入れ条件を追加する。
  ingest / refresh / build-index 実行後も `git status` に runtime state が出ないこと。
  既定で `./.local-knowledge-layer/index/knowledge.db` と `./.local-knowledge-layer/knowledge/...` が生成されること。
  `LOCAL_KNOWLEDGE_STATE_DIR` 指定時にはその場所へ保存先が切り替わること。
  検索順序 `project -> skills -> external/shared -> web` が維持されること。

- `Artifacts and Notes` に短い注記を追加する。
  現行 README の導入手順は Skill インストールまでは正しいが、runtime knowledge の保存戦略は次フェーズで `workspace hidden state + .gitignore` 前提へ更新する予定。

## Test Plan

- ExecPlan に次フェーズの追加テスト予定として記載する。
  `state-root` 解決の単体テスト。
  `LOCAL_KNOWLEDGE_STATE_DIR` 優先順位テスト。
  hidden state 配下への ingest / build-index / refresh のスモークテスト。
  `git status` に runtime state が現れないことの確認。

## Assumptions

- 既定運用は 1 workspace ごとに 1 つの hidden state を持つ。
- cross-workspace 共通キャッシュは v1.1 以降の拡張とし、初期実装では必須にしない。
- 既存 ExecPlan は置き換えではなく追記更新でよい。
- 実際のファイル更新時は `[.agent/2026-03-06-local-knowledge-layer-execplan.md](C:\Users\Hodaka\Downloads\div\local-knowledge-layer\.agent\2026-03-06-local-knowledge-layer-execplan.md)` の `Progress`、`Decision Log`、`Plan of Work`、`Validation and Acceptance` を同期して更新する。
